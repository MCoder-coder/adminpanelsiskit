import { DynamicTablesDirective } from './dynamic-tables.directive';

describe('DynamicTablesDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicTablesDirective();
    expect(directive).toBeTruthy();
  });
});
